# Auth验证

Auth验证模块提供一些控制器、模型和助手类来帮你完成用户的登陆和验证等功能。

激活的过程就不多说，直接在[Kohana::init]中添加`Auth`即可。

接下来，你就需要对这个模块进行一些必要的[设置](config)了。

在Kohana原始版本中，Auth模块只提供了[Auth::File]驱动器。
而在团队使用的版本中，因为我们对模块加载部分进行了改造了，所以此模块集成了`ORM`模块中的Auth驱动器。

如果你觉得有需要，你也可以[开发](driver/develop)属于你自己的驱动。
例如在我们的日程中，我们计划开发一个UCenter驱动。
